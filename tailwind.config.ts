import type { Config } from "tailwindcss";

export default <Partial<Config>>{
  theme: {
    extend: {
      colors: {
        mPink: "#f7adc8",
        mBlack: "#000000",
        mBlack2: "#111111",
        mYellow: "#fdca5a",
        mGray: "#e7e5e2",
      },
    },
  },
};
